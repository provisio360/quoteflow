# better-auth lists @sveltejs/kit as an OPTIONAL peer, which transitively drags in
# a vite 8 beta that conflicts with vitest's vite 7. We don't use SvelteKit, so the
# conflict is spurious. legacy-peer-deps keeps `npm install`/`npm ci` working in
# dev and CI without that false resolution error.
legacy-peer-deps=true
